<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Arcgis Server | Josh Werts]]></title>
  <link href="http://joshwerts.com/blog/categories/arcgis-server/atom.xml" rel="self"/>
  <link href="http://joshwerts.com/"/>
  <updated>2015-12-02T19:04:11-05:00</updated>
  <id>http://joshwerts.com/</id>
  <author>
    <name><![CDATA[Josh Werts]]></name>
    <email><![CDATA[joshwerts@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Feature Service applyEdits on Multiple Layers]]></title>
    <link href="http://joshwerts.com/blog/2015/11/10/feature-service-applyedits-on-multiple-layers/"/>
    <updated>2015-11-10T13:09:37-05:00</updated>
    <id>http://joshwerts.com/blog/2015/11/10/feature-service-applyedits-on-multiple-layers</id>
    <content type="html"><![CDATA[<p>If you&rsquo;ve worked with any of the esri web API&rsquo;s over the last several years, chances are you&rsquo;ve used a Feature Service to edit an SDE feature class at some point.  Using the web API&rsquo;s, the obvious way to do this would be to construct a <a href="https://developers.arcgis.com/javascript/jsapi/featurelayer-amd.html">FeatureLayer</a> and call its <a href="https://developers.arcgis.com/javascript/jsapi/featurelayer-amd.html#applyedits">applyEdits</a> method.  In reality, this is just a wrapper around the ArcGIS Server REST API&rsquo;s applyEdits endpoint on an individual service layer.</p>

<p><em>Example:</em></p>

<p><code>
http://[host]/arcgis/rest/services/[folder]/[service]/[layerid]/FeatureServer/applyEdits
</code></p>

<p>But what happens if you want to applyEdits to multiple layers within the Feature Service instead of just one layer?  You&rsquo;ve got a few standard API options after constructing multiple feature layers:</p>

<ul>
<li>Call applyEdits on each FeatureLayer with <a href="https://dojotoolkit.org/api/?qs=1.10/dojo/promise/all">dojo/promise/all</a>:</li>
</ul>


<p>```js
// construct FeatureLayers
flayer1 = new FeatureLayer(&ldquo;<a href="http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/0">http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/0</a>&rdquo;);
flayer2 = new FeatureLayer(&ldquo;<a href="http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/1">http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/1</a>&rdquo;);</p>

<p>// call applyEdits
var deferred1 = flayer.applyEdits(adds1, deletes1, updates1);
var deferred2 = flayer.applyEdits(adds2, deletes2, updates2);</p>

<p>// use dojo/All to respond to success or failure when both operations finish
all([deferred1, deferred2]).then(function(results) {
  // Do something with results.
  // Note that you&rsquo;ll still need to check each edit in each layer for &ldquo;success&rdquo;
}, function(error) {
  // uh oh, one of them failed.<br/>
});
```</p>

<ul>
<li>Chain promises for each FeatureLayer:</li>
</ul>


<p>```js
// construct FeatureLayers
flayer1 = new FeatureLayer(&ldquo;<a href="http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/0">http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/0</a>&rdquo;);
flayer2 = new FeatureLayer(&ldquo;<a href="http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/1">http://MyServer/arcgis/rest/services/MyServiceFolder/MyService/1</a>&rdquo;);</p>

<p>// call applyEdits
flayer.applyEdits(adds1, deletes1, updates1).then(function(result1) {
  // do something with result
  // Note that you&rsquo;ll still need to check each edit for &ldquo;success&rdquo;
  flayer2.applyEdits(adds2, deletes2, updates2).then(function(result2) {</p>

<pre><code>// do something with result
// Note that you'll still need to check each edit for "success"
</code></pre>

<p>  }, function(error) {</p>

<pre><code>// flayer 2 error
</code></pre>

<p>  });
}, function(error) {
  // flayer 1 error
});
```</p>

<p><strong>Either of these options would work, but what if the 2 layer edits depended on each other?</strong></p>

<p>Usually when I&rsquo;m editing multiple layers at once, they have some sort of relation to each other.  This may or may not be defined in the database, but at any rate &ndash; if one edit fails, I&rsquo;d really like the other edit to also fail so we don&rsquo;t end up with an inconsistency in the database.  Additionally, wouldn&rsquo;t it be nice to only send one request to the server, especially when there&rsquo;s many layers?</p>

<h3>One Request to Rule Them All</h3>

<p>It turns out that the ArcGIS Server REST API also has an <a href="http://resources.arcgis.com/en/help/rest/apiref/fsserviceedits.html">applyEdits</a> endpoint on the Feature Service itself &ndash; it&rsquo;s just not wrapped in the web API&rsquo;s.</p>

<p>This makes it possible to send multiple edits in a single request AND make sure nothing gets stored on any layers if any one of the layer edits fails.</p>

<p>There&rsquo;s nothing in the ESRI web API&rsquo;s that directly hit this endpoint, so I give to you: FeatureService.js.</p>

<p><code>
bower install featureservice-esri-jsapi
</code></p>

<h4>Usage:</h4>

<p>```js
var package_path = window.location.pathname.substring(0, window.location.pathname.lastIndexOf(&lsquo;/&rsquo;));
var dojoConfig = {
  async: true,
  packages: [</p>

<pre><code>{
  name: 'feature-service',
  location: package_path + '/js/lib/bower_components/featureservice-esri-jsapi/dist',
  main: 'FeatureService.min'
}
</code></pre>

<p>  ]
};</p>

<p>require(
  [</p>

<pre><code>'feature-service'
</code></pre>

<p>  ], function(FeatureService) {</p>

<pre><code>'use strict';

var service = new FeatureService("http://[server]/arcgis/rest/MyService");
var edits = [
  {
    id: 2,  // id of layer in service
    adds: [
      addGraphic
    ],
    updates: [
      updateGraphic
    ],
    deletes: [
      1, 2  // just objectids of features to delete.
    ]
  },
  {
    id: 5,  // id of layer in service
    adds: [
      someGraphic,
      anotherGraphic,
      yetAnotherGraphic
    ]
  }
];

var deferred = service.applyEdits(edits);
deferred.then(function(result) {
  // result is an array
  // each array value contains an object with layer id and objectids of successful edits:
  /*
  [
    {
      id: int id of feature service layer
      adds: [oid, oid, oid, ...],
      updates: [oid, oid oid, ...],
      deletes: [oid, oid, oid, ...]
    },
    ...
  ]
  */
  // layer 2 results
  console.log(result[0].id);
  console.log(result[0].adds);
  console.log(result[0].updates);
  console.log(result[0].deletes);

  // layer 5 results
  console.log(result[1].id);
  console.log(result[1].adds);
  console.log(result[1].updates);
  console.log(result[1].deletes);

}, function(error) {
  /* error callback can be called for 2 reasons
    1. Regular service faults (server 500 error, etc)
    2. One or more of the edits failed (but server still responded with 200)

    In case 1, the error is a standard esri error object with code and message.
    In case 2, the error is an object with code (200) and message plus an additional
    errors property which contains an array of error objects.
    Error object contains code, description, and id (layer id).
  */
  // case 2
  if (error.code === 200) {
    for (var i; i&lt;error.errors.length; i++) {
      var err = error.errors[i];
      console.log(err.id);
      console.log(err.code);
      console.log(err.description);
    }
  }
});
</code></pre>

<p>});</p>

<p>```</p>

<p>Currently only the applyEdits endpoint is implemented, but this is really the most useful.  There are a couple of personal design decisions here:</p>

<ul>
<li>The service always sends rollbackOnFailure as true.  This seems to be the most likely use case.</li>
<li>If any single edit fails, the fault handler is triggered.  This is different from the jsapi&rsquo;s FeatureLayer, where even if there&rsquo;s a failure, it will still trigger the success handler and you have to check each result for failures.  This class does it for you.</li>
</ul>


<p>If you find this useful, please leave a comment or feel free to fork!  <br/>
<a href="https://github.com/jwerts/featureservice-esri-jsapi.git">https://github.com/jwerts/featureservice-esri-jsapi.git</a></p>

<p>As a side note, this could have been a separate post on <em>&ldquo;Unit testing esri Javascript API services with Karma/Mocha/Chai/Sinon&rdquo;</em>.  Perhaps in the future&hellip;</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Debugging Server Object Extensions (SOEs) in Style]]></title>
    <link href="http://joshwerts.com/blog/2015/04/12/debugging-server-object-extensions-soe-in-style/"/>
    <updated>2015-04-12T17:09:42-04:00</updated>
    <id>http://joshwerts.com/blog/2015/04/12/debugging-server-object-extensions-soe-in-style</id>
    <content type="html"><![CDATA[<p>Server Object Extensions (SOEs) can be incredibly powerful, but they can also be a pain to debug.</p>

<h4>Typical workflow:</h4>

<ol>
<li>Rebuild solution</li>
<li>Open ArcGIS Manager in browser &ndash;> Site &ndash;> Extensions &ndash;> Update Extension by selecting .soe file.</li>
<li>Restart all of the services that depended on the SOE.</li>
<li>Attach Visual Studio to process.</li>
</ol>


<p>Steps 2 &amp; 3 are the really annoying ones, but they can be automated!</p>

<p>It turns out this is really a post about REST and Python.</p>

<p>When you update an SOE through the ArcGIS Manager app (<a href="http://localhost/arcgis/manager">http://localhost/arcgis/manager</a>), the web application is actually making calls to ArcGIS Server REST admin endpoints.</p>

<h3>Automate with Python</h3>

<p>I&rsquo;ll cut to the chase.</p>

<p><strong>See the full script here:</strong><br/>
<a href="https://github.com/jwerts/soe-updater">https://github.com/jwerts/soe-updater</a></p>

<p>Usage (simply run from command line after rebuilding SOE solution):
```
C:\Projects_General_Code\soe-updater>python update_soe.py</p>

<p>Retrieving token&hellip;
Retrieved: oUK04q-J8ORWDUrSWGPfq8zAU29u3q5_YZ79ZvcFZx8kFneOMb5Z2Y2Yf19
Uploading SOE&hellip;
Uploaded: ibd792bae-a986-4861-8ac3-c16d42f4d610
Updating SOE&hellip;
Updated!
Starting services&hellip;
Starting service_folder/service.MapServer
Started!
```</p>

<p>Your SOE will be updated and services restarted.  You can re-attach to the service processes or if you&rsquo;re using <code>System.Diagnostics.Debugger.Launch();</code> in your <code>Init()</code> function, you&rsquo;ll automatically be greeted with the dialog to attach your process once the script finishes and the services restart.</p>

<br/><br/>


<p>Now a little explanation of what&rsquo;s going on&hellip;</p>

<h3>REST API Calls</h3>

<h4>Generate token</h4>

<p>Because we&rsquo;re going to automate this, we need a preliminary step.  We&rsquo;ll generate a token that can be passed to subsequent steps.
<code>
http://localhost/arcgis/rest/tokens
</code></p>

<h4>Upload the SOE</h4>

<p>The .soe file that gets generated after building in Visual Studio is uploaded to ArcGIS Server and returns an itemID.
<code>
http://localhost/arcgis/admin/uploads/upload
</code></p>

<h4>Update the SOE</h4>

<p>Another call references the uploaded .soe through the itemID and updates the extension.
<code>
http://localhost/arcgis/admin/services/types/extensions/update
</code></p>

<h4>Restart services</h4>

<p>After the SOE is updated, all services that use it are stopped and must be restarted.  This can also be accomplished through REST calls:
<code>
http://localhost/arcgis/admin/services/[service_folder]/[service_name]/start
</code></p>

<p>These calls are all pretty straightford to automate in a Python script using the requests library (<a href="http://docs.python-requests.org/en/latest/">http://docs.python-requests.org/en/latest/</a>).</p>
]]></content>
  </entry>
  
</feed>
